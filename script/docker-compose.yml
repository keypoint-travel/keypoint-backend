version: "3.8"

services:
  web:
    container_name: web
    image: lchy0413/keypoint-travel-dev
    expose:
      - 8080
    ports:
      - 8080:8080
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"